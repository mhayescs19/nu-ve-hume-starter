export const getTwilioMessage = async (date: string, time: string, message: string): Promise<string> => {

    console.log("Scheduled message: " + message)
    // test code to extract EVI's generated notification message
    return message;
}